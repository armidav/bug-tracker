pipeline {
    agent any

    stages {
        stage('Unit Tests - Backend') {
            steps {
                dir('bugtracker-backed') {
                    sh 'go test -v ./...'
                }
            }
        }
    }
}
